[project]
name = "bgd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "kagglehub>=1.0.0",
    "pandas",
    "psycopg2-binary>=2.9.11",
    "pyarrow>=23.0.1",
]
